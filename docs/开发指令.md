@需求文档.md

请根据这份增强版需求文档，开发一个Web版的ValQ工具。

【项目根目录】
E:\MY AI\ValQ

【技术栈要求】
- 纯前端实现（HTML/JavaScript/CSS）
- 使用 React + D3.js 构建驱动树可视化
- 使用 Zustand 管理状态
- 使用 TailwindCSS 做样式
- 构建工具：Vite

【核心目标】
实现一个驱动型业务规划工具的 MVP（阶段一功能），让用户可以：
1. 创建驱动因子节点和计算节点
2. 通过公式（如 A * B）建立依赖关系
3. 用滑块调整驱动因子，所有依赖节点实时更新
4. 节点数值变化时，用绿色（增加）/红色（减少）标识
5. 可以导出/导入模型 JSON

【开发流程】
1. 请先阅读需求文档，理解 P0 优先级的功能
2. 输出一份技术实现方案和文件结构规划（包括主要组件和状态结构）
3. 我确认后，开始分步编码实现

【特别提醒】
- 计算引擎要基于依赖图实现，确保更新效率
- 每一步完成后，我会在浏览器中测试，请确保代码可以直接运行
- 使用中文注释，方便后续维护
- 功能方面可以参考https://valq.com/这里的描述
- 前端界面美化可以考虑使用frontend-design

请开始分析需求。